using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ConnectPoint : Point
{
   // Check which detectPoint type it collides with
   // it has to be the same as the tag
   // So the detectpoint has the tag thats passed in as a string
   // And the connectpoint must have the same tag as the detectpoint
   // Thats passed the same way as the detectpoint
}
